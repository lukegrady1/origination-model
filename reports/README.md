# Reports

`reports/final/` holds the committed, sanitized outputs of the frozen 2025 holdout run and the
development/confirmation runs it depends on (Markdown report, PNG charts, metrics JSON,
prediction CSV). Everything else under `reports/` is regenerated by
`uv run nfl-origination report --run <run-id>` and is not committed.
